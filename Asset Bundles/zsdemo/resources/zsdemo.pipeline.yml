# The main pipeline for zsdemo
resources:
  pipelines:
    zsdemo_pipeline:
      name: zsdemo_pipeline
      catalog: dbacademy
      schema: ${workspace.current_user.short_name}
      serverless: true
      libraries:
        - notebook:
            path: ${var.my_notebook_path}/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
