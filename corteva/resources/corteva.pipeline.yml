# The main pipeline for corteva
resources:
  pipelines:
    corteva_pipeline:
      name: corteva_pipeline
      catalog: ${workspace.current_user.short_name}
      schema: default
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
